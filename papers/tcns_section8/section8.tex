% T-CNS flagship §VIII (numerics) — Tier-1-ONLY rendition per the publication
% split (R9 defense: the multibody campaign is the RA-L companion; a forward
% pointer only appears here). \input into the flagship, or compile
% standalone.tex to preview. Registry captions verbatim where mandated.
\section{Numerical verification}\label{sec:numerics}

All cells in this section run on the reduced plant inside protocol class
$\mathcal{P}$ (uniform lags, zero jitter and loss, realized age asserted equal
to $\tau$ per fusion event), with the estimator, metric functional, window
rules, and fit code shared verbatim with the multibody companion --- so
agreement between the two papers is a statement about the algorithm, and
disagreement localizes to the plant. Every claim below carries the pre-registered
falsifier it was exposed to; three fired, and their firings are reported as
findings. Every number traces to a seeded, versioned driver re-executed
exactly by the released replay harness.

\subsection{Plant, protocol, and information structure}
Figure~\ref{fig:plantF} defines the experimental object: $N$ agents on taut
unit cables to a common load, each carrying the shape pair
$s_j = (\sigma_j, \sigma_{i,j})$, communicating over a graph whose algebraic
connectivity $\lambda_2$ is a controlled variable (the $C_5$ cycle is the
default; $K_5$ and the E6 sweep vary it). Figure~\ref{fig:archF} is the
method pipeline actually executed: per agent, 50\,Hz odometry drives a
left-invariant propagation of the load-pose estimate through the slaved twist,
a 20\,Hz cable-direction channel updates the shape, and at every send epoch
the sheaf-weighted fusion rule transports a $\tau$-stale neighbor estimate
through the shape-dependent edge map before combining. The information
boundary is strict and machine-checked: each agent sees its own two sensors,
its neighbors' stale estimates, and (in pinning cells only) one absolute
beacon at one agent --- the plant's truth state reaches the estimator only
through the sensor models. Table~\ref{tab:configF} fixes every constant a
reimplementation needs; Table~\ref{tab:matrixF} is the executed cell matrix
of this section.

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/tcns_plant.pdf}
  \caption{The reduced plant and its communication structure: shapes
  $s_j = (\sigma_j, \sigma_{i,j})$ on taut unit cables; the comms graph
  (dashed) is a controlled variable (the $C_5$ default shown; $K_5$ and the
  E6 sweep vary $\lambda_2$).}
  \label{fig:plantF}
\end{figure}

\begin{figure*}[t]
  \centering
  \input{fig_arch.tex}
  \caption{The executed estimator pipeline and its information boundary:
  local sensing (odometry, cable direction), $\tau$-stale shared estimates
  transported through $\rho = \mathrm{Ad}_m \circ \pi_L$ with sheaf weights
  $w_e$, and at most one absolute anchor. Nothing else touches truth
  (linted).}
  \label{fig:archF}
\end{figure*}

\begin{table}[t]
\centering
\caption{Experimental configuration (Tier-1).}
\label{tab:configF}
\begin{tabular}{ll}
\toprule
plant integrator & RK4 shapes + exact $SE(2)$ load step, $\Delta t = 0.01$\,s
(E10 sweeps $10\times$) \\
send epoch $D_c$ & 0.05\,s; $\tau$ grid $\{0.05..1.6\}$ exact on the grid \\
fusion gain & $\alpha = \kappa D_c$, $\kappa = 2.0$ (record gain) \\
sensing & odom 50\,Hz (surge noise $0.01{+}0.02|v|$, sway $0.01$, gyro
$0.2^\circ$/s + bias walk); direction 20\,Hz \\
maneuver & persistent turn $\eta(t) = 0.15 + 0.10\sin(0.05 t)$ \\
formations & id-keyed draws, half-arc law matched to the multibody
attachment rays; broadside margin enforced \\
metric & all-pairs mean $\|\mathrm{Log}(G_i^{-1}G_j)\|^2$, last 30\% window \\
runs & E3b production: 1584 (12 formations $\times$ 10 seeds per $\tau$ +
controls) \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[t]
\centering
\caption{Executed cell matrix of this section.}
\label{tab:matrixF}
\begin{tabular}{lll}
\toprule
cell & varies & falsifier(s) \\
\midrule
E1 & anchor on/off & kernel dim $\ne 3$ \\
E3a + ext & $\tau$, formation, switch-offs & C7a/C8/C13; C15 \\
E3b & $\tau \times$ arm $\times$ formation $\times$ seed & C7b (\conjtag) \\
E3c & $\varepsilon$ off-symmetry, robust arm & C9b$'$/C9b/C9c \\
E2 & $\kappa$, topology, $\|\delta\|$ & C6, C19 \\
E6 & topology $\times N$ ($\lambda_2$) & C10 (exploratory) \\
E10 & $\Delta t$ ($10\times$) & CI disjointness \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Gauge structure and pinning (E1; Thm.~\ref{thm:gaugeF}, Cor.~\ref{cor:pinF})}
On the triangle with generic shapes, the sheaf Laplacian is PSD with
$\dim\ker L_\mathcal{F} = 3$ and the gauge section
$(\mathrm{Ad}^{-1}_{m(s_j)} c)_j$ annihilated to $1.3\times10^{-15}$;
$\sigma_4/\sigma_3$ confirms a clean spectral gap. Adding a single anchor
potential collapses the kernel to zero and $\lambda_{\min}$ becomes the
convergence rate --- the pinning corollary as linear algebra
(Fig.~\ref{fig:f3}).
\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/tcns_e1_spectrum.pdf}
  \caption{Gauge spectrum: $\dim\ker L_\mathcal{F} = 3$ without an anchor,
  collapsing to zero with one anchor whose $\lambda_{\min}$ becomes the
  rate. (The harmonic-section identity --- kernel $=$ body-framed images of
  one common load-frame perturbation --- is the $1.3\times10^{-15}$
  annihilation check reported in the text.)}
  \label{fig:f3}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/tcns_gauge_trails.pdf}
  \caption{The gauge phenomenon as trajectories: with no anchor the agents'
  load-pose estimates (colours) track each other but drift as a rigid group
  off the truth (black); at the beacon (stars) they snap back (dashed).}
  \label{fig:gaugestory}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/tcns_gauge_errors.pdf}
  \caption{The two error objects are distinct: the disagreement $D$ (agents
  vs.\ each other) runs about an order of magnitude below the gauge error
  (agents vs.\ truth); one anchor collapses both (Thm.~5.1, Cor.~5.2).}
  \label{fig:gaugeerr}
\end{figure}

\subsection{The theorem's object: amplitude, coefficient, switch-offs
(E3a; Thm.~\ref{thm:floorF})}
The measured amplitude $\|\mathrm{Log\,Hol}\|$ on the $m{=}2$ round trip fits
slope $1.999$ over $\tau \in [0.05, 1.6]$\,s with coefficient ratio
$\|\mathrm{Log\,Hol}\|/\tau^2\|[C_i,C_j]\| = 1.0000$ at the smallest $\tau$;
the switch-off arms sit at machine zero ($s_i{\equiv}s_j$: $<10^{-15}$;
$\eta{=}0$: $<10^{-16}$; $\xi{=}0$: exactly $0.0$; explicit $\tau{=}0$ arm:
exactly $0.0$). Extension arms sharpen this: the slope is
formation-invariant across 20 random taut formations ($1.9993 \pm 0.0000$,
Fig.~\ref{fig:f4aext}); the remainder constant is uniformly tight
($\sup_{s,\tau} \|R\|/\tau^3 \le 0.0133$ over 220 draws); and a search for
zero-commutator pairs at $\ge 1$\,rad shape separation found only pairs with
$C_i = C_j$ to $10^{-16}$ (Fig.~\ref{fig:c15tc}) --- the protected class of
Cor.~\ref{cor:symF} extends, empirically, to the level set of
$s \mapsto C(s;\xi)$, where the full holonomy vanishes at all orders.
\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/tcns_amplitude.pdf}
  \caption{F4a: the leading-order, deterministic holonomy amplitude of
  Thm.~\ref{thm:floorF} --- slope 1.999, coefficient 1.0000, switch-offs at
  machine zero.}
  \label{fig:f4a}
\end{figure}
\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/tcns_formations.pdf}
  \caption{E3a formation invariance: 20 random taut formations overlaid ---
  the geometry varies widely, yet every one fits slope $1.9993$. The $\tau^2$
  order is a structural constant.}
  \label{fig:f4aext}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/lcss_levelset_bars.pdf}
  \caption{E3a level-set protection (C15): the five zero-commutator pairs at
  $\ge 1$\,rad separation all satisfy $C_i = C_j$ to $10^{-16}$ --- the
  protected class is a level set, not just coincident shapes.}
  \label{fig:c15tc}
\end{figure}

\subsection{The conjectured closed-loop floor, falsified in its own regime
(E3b)}
\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/tcns_f4b.pdf}
  \caption{Numerical evidence for the conjectured stochastic steady-state
  floor ([ES \S10(ii)]); Theorem~\ref{thm:floorF} is leading-order and
  deterministic and is tested in Fig.~\ref{fig:f4a}. Fitted excess exponent
  1.101 [1.076, 1.125]: the conjectured order 2 is excluded (and so is 1);
  the frozen+noise-off regression reproduces the executed null
  ($D_{ss} \sim 10^{-30}$).}
  \label{fig:f4b}
\end{figure}
With the full DIEKF-$\Sigma$ on the reduced plant integrating the shape ODEs
along the persistent-turn reference (1584 runs; 12 formations $\times$ 10
seeds per $\tau$ point; controls on matched draws), the disagreement excess
over the frozen-shape control fits exponent $1.101\,[1.076, 1.125]$
(formation-cluster CI $[1.070, 1.132]$): the \conjtag{} extension of the floor
to the noisy closed loop is falsified at these scales, coherently with the
multibody companion (cross-tier exponent difference $+0.023\,[-0.012,
+0.057]$). The mechanism check passes --- the straight-tow control sits
$\approx 5\times$ below the paper arm and the frozen-shape control
$\approx 15\times$ below, at every $\tau$ --- and the executed null pins the
harness: shapes frozen and noise off give $D_{ss} \sim 10^{-30}$.

Figures~\ref{fig:dssdist} and~\ref{fig:dssbox} expose the \emph{distributions}
the fitted exponent summarizes. At the reference staleness $\tau = 0.4$
(Fig.~\ref{fig:dssdist}) the arm separation is distributional, not just in
medians: the paper-rule and frozen-shape CDFs do not overlap at any quantile.
The paper-rule spread across the $\tau$ grid (Fig.~\ref{fig:dssbox}) is
$\approx 1.6\times$ interquartile, $\approx 3.7\times$ between the 5th and 95th
percentiles --- comparable to one $\tau$ octave, which is exactly why the
campaign pre-registered seed-paired estimators for the $\varepsilon$-response
(below) and why unpaired CIs were declared under-powered.

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/tcns_dss_cdf.pdf}
  \caption{Per-run $D_{ss}$ CDFs by arm at $\tau = 0.4$ (1584-run production
  record). $D_{ss}$ is the stochastic steady-state object [\conjtag{} regime]
  --- measured, not a test of Thm.~\ref{thm:floorF}.}
  \label{fig:dssdist}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/tcns_dss_box.pdf}
  \caption{Paper-rule $D_{ss}$ spread across seeds and formations per $\tau$
  ([\conjtag{}] regime).}
  \label{fig:dssbox}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=0.82\linewidth]{figures/tcns_floor_dyn.pdf}
  \caption{The floor's dynamics: the disagreement $D(t)$ climbs from a
  perturbation to its steady state $D_{ss}$ (dots) over a sweep of staleness
  $\tau$, higher for larger $\tau$, inside the shaded evaluation window. This
  is the measurement protocol for the [\conjtag{}] steady-state object; it is
  not a test of Thm.~\ref{thm:floorF}, whose object is the amplitude of
  Fig.~\ref{fig:f4a}.}
  \label{fig:floordyn}
\end{figure}

\subsection{Symmetry: two metrics, two exponents (E3c; Cor.~\ref{cor:symF})}
The amplitude departure from the symmetric class is first-order in
$\varepsilon$ (measured 1.006; base commutator exactly zero). The closed-loop
floor excess under the seed-paired estimator fits $\varepsilon$-exponent
$1.58\,[1.44, 1.84]$ at every $\tau$ --- excluding both the conjectured 2 and
1, with segment slopes rising $1.2 \to 2.1$ up the ladder: a
linear-plus-quadratic mixture that the registered single-power model
mis-specifies (reported per the outcome ladder). The robust
$\ge 10\times$ suppression falsifier trips ($2.75\times\,[1.94, 3.89]$ under
20\% jitter and 10\% loss): large symmetric protection is a property of the
amplitude object only (Fig.~\ref{fig:f5tc}). Figure~\ref{fig:robustF} shows
the tripping row at run level --- the full generic and symmetric $D_{ss}$
distributions under the robust arm. The registered statistic is the ratio of
arm \emph{means}, $2.75\times$ [1.94, 3.89] (the boxes display medians,
whose ratio is $3.2\times$): real, but an order short of the registered
$10\times$, and
explicitly outside protocol class $\mathcal{P}$ (heterogeneous realized
ages), so it characterizes robustness rather than testing the theorem.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.75\linewidth]{figures/tcns_robust.pdf}
  \caption{The C9c trip at run level: generic vs.\ symmetric-class $D_{ss}$
  under 20\% jitter + 10\% drops (outside class $\mathcal{P}$). Median ratio
  $2.75\times\,[1.94, 3.89]$ --- protection persists but falls an order short
  of the registered $10\times$.}
  \label{fig:robustF}
\end{figure}
\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/tcns_f5_amp.pdf}
  \caption{Symmetry, the amplitude object (C9b$'$): the departure from the
  symmetric class is first-order in $\varepsilon$ (slope 1.006), the base
  commutator exactly zero --- PASSED.}
  \label{fig:f5tc}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/tcns_f5_floor.pdf}
  \caption{Symmetry, the closed-loop floor (C9b, [\conjtag{}] regime): the
  seed-paired $\varepsilon$-exponent is $1.58$ at every $\tau$, excluding both
  2 and 1 --- falsifier MET, a linear-plus-quadratic mixture the registered
  single-power model mis-specifies. Never mixed with the amplitude object.}
  \label{fig:f5floor}
\end{figure}

\subsection{Contraction in its own domain (E2; Thm.~\ref{thm:contractF})}
\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/tcns_f8.pdf}
  \caption{F8: rate versus $\kappa\lambda_2$ across two topologies, slope
  1.40 (falsifier $< 0.5$); $\mu$ calibrated at $\kappa = 0$, so the identity
  line is anchored, not predicted; the nonlinear extension is open.}
  \label{fig:f8tc}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/tcns_contraction_xy.pdf}
  \caption{Contraction as trajectories: after a step perturbation, each
  agent's gauge-complement error spirals into consensus (the origin), faster
  under the more connected $K_5$ than the $C_5$ cycle (LTV/frozen regime).}
  \label{fig:contportrait}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/tcns_contraction_decay.pdf}
  \caption{The gauge-complement decay: its rate scales with $\kappa\lambda_2$
  (C6, slope 1.403), faster on the more connected graph.}
  \label{fig:contdecay}
\end{figure}
Step-perturbing all agents and fitting the decay of the gauge complement:
the rate scales with $\kappa\lambda_2$ at slope 1.403 across a $4\times$ gain
range and two topologies, with $\mu = -0.062$ topology-independent. The
log-linearity remainder vanishes \emph{faster} than the registered second
order (scaling-pair exponent 2.48 [2.32, 2.65]): the falsifier band is
excluded on the favorable side --- reported as tripped, with the corrected
order left to a restatement.

\subsection{Numerics invariance (E10)}
The excess exponent is invariant across integrator steps
$\Delta t \in \{0.001, 0.0025, 0.005, 0.01\}$\,s (all CIs share
$[1.185, 1.256]$), with the coefficient converging mildly under refinement:
no exponent above is an artifact of the discretization
(Fig.~\ref{fig:dtF}).

\begin{figure}[t]
  \centering
  \includegraphics[width=0.8\linewidth]{figures/tcns_dt.pdf}
  \caption{E10: the fitted exponent across a $10\times$ range of integrator
  steps; all CIs share the green band $[1.185, 1.256]$.}
  \label{fig:dtF}
\end{figure}

\subsection{Connectivity and the floor (E6; exploratory)}
Pre-registered as exploratory (C10, no falsifier): sweeping topology and
fleet size over cycle/path/star/complete at $N \in \{4, 6, 8\}$, connectivity
suppresses the floor by up to $5\times$ at moderate staleness, and the
benefit collapses ($\le 2\times$) at high staleness, with complete graphs the
most $\tau$-sensitive (Fig.~\ref{fig:topoF}). Read forward, this is a design
observation: buying links buys floor suppression only while the staleness is
moderate --- at high $\tau$ every topology pays the same curvature price. The
anchoring-side counterpart (whether connectivity buys \emph{pin-rate}) is a
multibody question and is answered negatively in the companion.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.85\linewidth]{figures/tcns_topology.pdf}
  \caption{E6 (exploratory, C10): floor vs.\ topology$\times N$ ordered by
  $\lambda_2$, at moderate ($\tau{=}0.2$) and high ($\tau{=}0.8$) staleness.
  Connectivity suppresses the floor $5\times$ at moderate $\tau$; the benefit
  collapses at high $\tau$.}
  \label{fig:topoF}
\end{figure}

\subsection{Switch-off ladder and the transport-rule ablation}
\begin{table}[t]
\centering
\caption{Switch-off ladder (Tier-1 rows). Each arm kills the effect on
command or shifts its order as predicted.}
\begin{tabular}{lcc}
\toprule
arm & prediction & measured \\
\midrule
generic slope & $2$ & $1.999$ (formation-invariant $1.9993 \pm 0.0000$) \\
coefficient ratio ($m{=}2$) & $1$ & $1.0000$ \\
$s_i \equiv s_j$ & $0$ & $< 10^{-15}$ \\
level set $C_i = C_j$, sep $\ge 1$ & --- & full holonomy $\equiv 0$ (observed) \\
$\eta = 0$ & $0$ & $< 10^{-16}$ \\
$\xi = 0$ & $0$ & $0.0$ \\
$\tau = 0$ (explicit arm) & $0$ & $0.0$ \\
frozen shapes, closed loop & null & $D_{ss} \sim 10^{-30}$ \\
\bottomrule
\end{tabular}
\end{table}

Figure~\ref{fig:ladderF} adds the closed-loop transport-rule ablation
(E3b arms) as median $D_{ss}$ per arm and staleness, and it contains a
finding we flag rather than smooth over: on \emph{this} plant, the A1
ablation (no lag compensation --- the rule-registry semantics; A2 is the
un-conjugated transport) sits \emph{below} the paper rule on $D_{ss}$ ---
the ordering inverts relative to the multibody plant, where the transport
ablations degrade correctness $1.7$--$8.2\times$. The resolution is the
metric, not the rule: $D_{ss}$ is a \emph{disagreement} functional, and
agreement is purchasable by groupthink; it never ranks rules. Rule ranking
belongs to correctness metrics against truth, which exist only on the
companion's plant, where the ordering is clean. We show the inversion because
suppressing it would misrepresent what $D_{ss}$ measures.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.9\linewidth]{figures/tcns_ladder.pdf}
  \caption{E3b transport-rule and control arms, median $D_{ss}$ by staleness
  [\conjtag{} object]. The Tier-1 A1-below-paper inversion is shown, not
  smoothed: $D_{ss}$ is a disagreement metric and never ranks rules ---
  ranking is done on the companion's truth-referenced metrics. The
  noise-off frozen null ($D_{ss} \sim 10^{-30}$) lies 27 orders below the
  axis and is reported in the table.}
  \label{fig:ladderF}
\end{figure}

\subsection{The adjudicated ledger, in one figure}
Figure~\ref{fig:forestF} renders the section's adjudicated falsifier rows
--- every quantitative falsifier this section reports --- with registered
references and verdicts: the statistical integrity statement. (The
structural E1 falsifier, kernel dimension $\ne 3$, is pass/fail rather than
an estimate and is reported in Sec.~E1.) The proved chain's rows (C7a slope, C9b$'$, C6, E10)
are green; the \conjtag{} regime's rows are red where the falsifier fired
(C7b, C9b, C9c), exactly as reported in the subsections above; C19 is orange:
tripped in the favorable direction. Nothing is restated as a pass, and the
one-axis view makes the epistemic split of the paper visible at a glance:
the theorem's objects survive every registered attack; the conjectured
stochastic extension does not, and is reported as measured.

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/t1_falsifier_forest.pdf}
  \caption{The complete Tier-1 falsifier ledger, verdicts as adjudicated:
  green passed, red falsified/met (\conjtag{} rows --- measured slopes
  reported as measured), orange tripped favorably. Grey marks the registered
  references; the evidence record is named per row.}
  \label{fig:forestF}
\end{figure}

\subsection{Supplementary movies}
Three supplementary movies accompany this section; each is generated by a
seeded, versioned script from the same estimator code as the campaign.
\emph{M1 (gauge\_drift.mp4)} --- the E1 phenomenon: three agents fuse without
an anchor, remain mutually consistent, and drift \emph{as a group} along the
$SE(2)$ gauge; a beacon at one agent collapses the gauge and all three snap
to truth. The two curves it traces --- disagreement vs.\ truth error --- are
the two different objects this paper refuses to conflate. (Playback: 8\,fps,
time-compressed; the on-screen clock is authoritative.)
\emph{M2 (contraction.mp4)} --- the E2 mechanism: identical step
perturbations decaying under $C_5$ and $K_5$ side by side; per-agent
gauge-complement errors shrink toward consensus at rates ordered by
$\kappa\lambda_2$ (C6's tested prediction; LTV/frozen hedge carried
on-screen). (Playback: 15\,fps; 6\,s of simulated decay over $\approx$8\,s
of video.)
\emph{M3 (floor\_protocol.mp4)} --- the E3b \emph{protocol}: the reduced
plant executes the persistent turn while $D(t)$ for two stalenesses rises to
its steady state inside the declared evaluation window. (Playback: 25\,fps;
120\,s of simulation in $\approx$24\,s, $\approx$5$\times$ compression.) The
on-screen caption carries the registry hedge verbatim: $D_{ss}$ is measured
in the [\conjtag{}] regime; the movie demonstrates the protocol and is not a
test of Thm.~\ref{thm:floorF}.

\subsection{Reproducibility and artifact registry}
Every number in this section traces to a seeded, versioned driver and a
committed record; the released \texttt{campaign\_replay.py} re-executes
per-run probes that must match the records exactly (Tier-1 to $10^{-9}$).
Table~\ref{tab:registryF} is the executed registry: cell, driver, committed
record, and probe class. The figure generator
(\texttt{tier1\_sheaf/campaign/paper\_artifacts.py}) re-derives the
extension datasets seed-exactly at import and refuses to plot on mismatch,
so the figures cannot drift from the records. The repository is released
with the paper (archived tagged release with DOI at submission);
reproduction commands are in its root \texttt{README}. The multibody
campaign, its falsifications, and the cross-tier equivalence tests appear in
the RA-L companion.

\begin{table}[t]
\centering
\caption{Experiment registry (cell $\to$ driver $\to$ record $\to$ probe).}
\label{tab:registryF}
\begin{tabular}{llll}
\toprule
cell & driver (\texttt{tier1\_sheaf/}) & record & probe \\
\midrule
E1 & \texttt{experiments/e1\_gauge.py} & \texttt{e1\_gauge.csv} & exact \\
E3a & \texttt{experiments/e3a\_amplitude.py} & \texttt{e3a\_amplitude.csv} &
exact \\
E3a-ext & \texttt{campaign/e3a\_extension.py} & \texttt{e3a\_extension.json} &
exact \\
E3b & \texttt{campaign/e3b\_production.py} & \texttt{e3b\_production.json}
(1584) & exact \\
E3c & \texttt{experiments/e3c\_symmetry.py} + \texttt{campaign/e3c\_c9b\_v2.py}
& \texttt{e3c\_*.json} & exact \\
E2 & \texttt{experiments/e2\_contraction.py} & \texttt{e2\_contraction.json} &
stat \\
E6 & \texttt{campaign/e6\_topology.py} & \texttt{e6\_topology.json} & exact \\
E10 & \texttt{campaign/e10\_dt\_sweep.py} & \texttt{e10\_dt\_sweep.json} &
exact \\
movies & \texttt{campaign/*\_movie.py}, \texttt{experiments/anim\_gauge.py} &
\texttt{*.mp4} & seeded \\
\bottomrule
\end{tabular}
\end{table}
